package vn.edu.fit.iuh.repositories;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.data.jpa.repository.Query;import org.springframework.stereotype.Repository;import vn.edu.fit.iuh.enities.Candidate;import java.util.List;@Repositorypublic interface CandidateRepository extends JpaRepository<Candidate, Long> {    @Query("SELECT c FROM Candidate c LEFT JOIN FETCH c.address")    List<Candidate> findAllCandidatesWithAddress();  //Kiểm tra xem email đã tồn tại chưa    @Query("SELECT CASE WHEN COUNT(c) > 0 THEN true ELSE false END FROM Candidate c WHERE c.email = ?1")    boolean existsCandidateByEmail(String email);    //Lấy tên dựa vào email    @Query("SELECT c.fullName FROM Candidate c WHERE c.email = ?1")    String findNameByEmail(String email);}